import React from 'react';

const page = () => {
    return (
        <div>
            comments page from api server
            <p>it is a way to write api for every single page.</p>
        </div>
    );
};

export default page;